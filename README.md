# Habit Tracker Backend (Node.js + Express)

A simple Habit Tracker CRUD API with a small static web UI for testing.
Persistence: local JSON file (data/habits.json).

## Run
1. npm install
2. copy .env.example to .env and set values if needed
3. npm start
